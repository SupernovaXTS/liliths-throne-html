(function () {
  lt.races.horn = class Horn extends lt.races.bodyPart {
    // Abstract horn class. All horn parts should extend this class.
    constructor(opts) {
      super(opts);
    }
  };
})();
