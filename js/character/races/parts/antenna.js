(function () {
  lt.races.antenna = class Antenna extends lt.races.bodyPart {
    // Abstract antenna class. All antenna parts should extend this class.
    constructor(opts) {
      super(opts);
    }
  };
})();
