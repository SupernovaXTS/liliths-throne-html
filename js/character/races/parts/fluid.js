(function () {
  lt.races.fluid = class Fluid extends lt.races.bodyPart {
    // Abstract fluid class. All fluid parts should extend this class.
    constructor(opts) {
      super(opts);
    }
  };
})();
