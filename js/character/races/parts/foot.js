(function () {
  lt.races.foot = class Foot extends lt.races.bodyPart {
    // Abstract foot class. All foot parts should extend this class.
    constructor(opts) {
      super(opts);
    }
  };
})();
