(function () {
  lt.races.mouth = class Mouth extends lt.races.bodyPart {
    // Abstract mouth class. All mouth parts should extend this class.
    constructor(opts) {
      super(opts);
    }
  };
})();
