(function () {
  lt.races.tentacle = class Tentacle extends lt.races.bodyPart {
    // Abstract tentacle class. All tentacle parts should extend this class.
    constructor(opts) {
      super(opts);
    }
  };
})();
